class AppUrl {
  AppUrl._();

  // base url
  static const String baseUrl = "https://dev.api.diltalks.com/";

  // receiveTimeout
  static const Duration receiveTimeout = Duration(minutes: 2);

  // connectTimeout
  static const Duration connectionTimeout = Duration(minutes: 2);

  static const String login = 'login/';
}
